{
  "cassandra_server": "${CASSANDRA_SEED_SERVERS}",
  "cassandra_port": "9042",
  "source_keyspace": "${SOURCE_KEYSPACE}",
  "source_table_name": "${SOURCE_TABLE_NAME}",
  "target_keyspace": "${TARGET_KEYSPACE}",
  "target_table_name": "${TARGET_TABLE}",
  "cassandra_user": "${CASSANDRA_USER_ID}",
  "cassandra_password": "${CASSANDRA_PASSWORD}",
  "pulsar_home":"${PULSAR_HOME}",
  "region":"${REGION}",
  "truststore_password":"${KEYSPACES_TRUSTSTORE_PASSWORD}",
  "keyspace_secret_name":"${KEYSPACES_SECRET_NAME}",
  "keyspaces_sink_nar_file":"pulsar-io-cassandra-sink-1.0.0-SNAPSHOT.nar",
  "keyspaces_sink_topic_read_from":"${KEYSPACES_SINK_TOPIC_READ_FROM}"
}